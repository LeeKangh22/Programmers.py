def reverse(str):
    temp = ''
    for i in range(0, len(str) / 2):
        temp = str[i]
        str[i] = str[len(str) - i]
        str[len(str) - i] = temp

def three(n, str):
    i = 0
    while pow(3, i) >= n:
        i += 1
    str += ? # 해결중
    return n % pow(3, i)

def solution(n):
    answer = 0

    while three(n)
    

    return answer