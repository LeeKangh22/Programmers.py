def solution(numbers):
    answer = ''
    start = 0
    sum = ''
    temp = []
    for i in range(0, len(numbers)):
        start = numbers[i]
        for j in range(0, len(numbers)):
            if j == i:
                pass
            
    return answer